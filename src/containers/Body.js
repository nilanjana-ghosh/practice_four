import RestaurantContainer from "./RestaurantContainer";

const Body = () => {
    return (
        <div className="body-container">
            <RestaurantContainer />
        </div>
    );
};

export default Body;